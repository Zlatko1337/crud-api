spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/ingemarkdb
        username: postgres
        password: 1
         #Dont write credentials in config files like this
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true
            hibernate,dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true